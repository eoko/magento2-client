language: php
sudo: required

cache:
  directories:
    - $HOME/.composer/cache/files

matrix:
  fast_finish: true
  include:
    - php: 7.1

services:
  - docker

before_install:
- docker-compose up
- docker-compose run --rm setup
- composer install

script:
- composer test